{
  "name": "fern-definition-registry",
  "version": "0.0.0",
  "main": "lib/index.js",
  "repository": "git@github.com:fern-api/fern-definition-registry.git",
  "author": "Deep Singhvi <deep@buildwithfern.com>",
  "license": "MIT",
  "files": [
    "lib"
  ],
  "dependencies": {
    "@fern-fern/fdr-db-model-model": "0.0.1",
    "@fern-fern/fern-definition-registry-api-server": "0.0.2",
    "@prisma/client": "4.1.1",
    "@types/jest": "^28.1.6",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "@babel/core": "^7.18.9",
    "@babel/preset-env": "^7.18.9",
    "@babel/preset-typescript": "^7.18.6",
    "@types/express": "^4.17.13",
    "dotenv-cli": "^6.0.0",
    "jest": "^28.1.3",
    "prisma": "^4.1.1",
    "simple-progress-webpack-plugin": "^2.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  },
  "scripts": {
    "server": "ts-node src/server.ts ",
    "clean": "rm -rf ./lib && tsc --build --clean",
    "compile": "tsc --build",
    "test": "dotenv -e .env.test -- ./scripts/db-tests.sh",
    "dev:db:migrate": "dotenv -e .env.dev -- yarn prisma migrate deploy",
    "local:db:migrate": "dotenv -e .env.local -- yarn prisma migrate dev",
    "local:db:up": "docker-compose -f docker-compose.db.yml up -d",
    "local:db:down": "docker-compose -f docker-compose.db.yml down -d",
    "local:docker": "dotenv -e .env.local -- ./create_docker.sh local",
    "dev:docker": "dotenv -e .env.dev -- ./create_docker.sh"
  }
}
