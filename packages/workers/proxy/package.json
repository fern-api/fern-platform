{
  "name": "@fern-platform/proxy",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "cf-typegen": "wrangler types",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.6.12",
    "@cloudflare/workers-types": "^4.20250204.0",
    "@vitest/runner": "^2",
    "@vitest/snapshot": "^2",
    "typescript": "^5",
    "vitest": "^2",
    "wrangler": "^4.4.0"
  }
}
