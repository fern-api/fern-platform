{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "compile": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build", "^compile", "codegen", "//#fdr:generate"]
    }
  }
}
