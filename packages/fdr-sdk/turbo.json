{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "outputs": ["lib/**", "dist/**"],
      "dependsOn": ["^build", "codegen", "//#fdr:generate"]
    }
  }
}
