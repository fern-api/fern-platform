{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "outputs": ["lib/**", "dist/**"],
      "dependsOn": ["^compile", "^build", "codegen", "//#fdr:generate"]
    }
  }
}
