{
  "name": "@fern-platform/github",
  "version": "0.0.0",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fern-api/fern-platform.git",
    "directory": "packages/github"
  },
  "sideEffects": false,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf ./lib && tsc --build --clean",
    "compile": "tsc --build",
    "depcheck": "depcheck",
    "dev": "tsc --watch",
    "organize-imports": "organize-imports-cli tsconfig.json",
    "test": "vitest --run --passWithNoTests --globals"
  },
  "dependencies": {
    "octokit": "^3.2.0",
    "tmp-promise": "^3.0.3"
  },
  "devDependencies": {
    "@fern-platform/tsconfig": "workspace:*",
    "@types/node": "^18.7.18",
    "depcheck": "^1.4.3",
    "organize-imports-cli": "^0.10.0",
    "simple-git": "^3.24.0",
    "typescript": "5.4.3",
    "vitest": "^1.5.0"
  }
}
