// import { FernIr } from "@fern-fern/ir-sdk";
// import { ReactElement } from "react";
// import { PathParameter } from "./PathParameter";

// export function PathParameters({ parameters }: { parameters: FernIr.PathParameter[] }): ReactElement {
//     return (
//         <ul>
//             {parameters.map((parameter) => (
//                 <PathParameter key={parameter.name.originalName} parameter={parameter} />
//             ))}
//         </ul>
//     );
// }
